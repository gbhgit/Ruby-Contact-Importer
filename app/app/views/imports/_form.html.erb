<%= form_with(model: import) do |form| %>
  <% if import.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(import.errors.count, "error") %> prohibited this import from being saved:</h2>

      <ul>
        <% import.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mt-3">
    <h2>Header</h2>
    <div class="field">
      <%= form.label 'header[name]', 'Name' %>
      <%= form.text_field 'header[name]', placeholder: "Enter corresponding header", required: true %>
    </div>
    <div class="field">
      <%= form.label 'header[birthdate]', 'Birthdate' %>
      <%= form.text_field 'header[birthdate]', placeholder: "Enter corresponding header", required: true %>
    </div>
    <div class="field">
      <%= form.label 'header[phone]', 'Phone' %>
      <%= form.text_field 'header[phone]', placeholder: "Enter corresponding header", required: true %>
    </div>
    <div class="field">
      <%= form.label 'header[address]', 'Address' %>
      <%= form.text_field 'header[address]', placeholder: "Enter corresponding header", required: true %>
    </div>
    <div class="field">
      <%= form.label 'header[address]', 'Credit card' %>
      <%= form.text_field 'header[credit_card]', placeholder: "Enter corresponding header", required: true %>
    </div>
    <div class="field">
      <%= form.label 'header[address]', 'Email' %>
      <%= form.text_field 'header[email]', placeholder: "Enter corresponding header", required: true %>
    </div>
  </div>

  <div class="field">
    <%= form.label :file %>
    <%= form.file_field :file %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
